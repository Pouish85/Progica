<div>
    <div class="min-h-min h-40 bg-black mt-1 flex flex-row items-center justify-center rounded-lg" >
        {# <form class="flex flex-row justify-between pt-10 px-10"> #}
            <div class="text-white flex flex-row justify-center items-start text-center space-x-4">
                {{ form_start(form, {'attr': { 'class': 'flex flex-row justify-center items-start text-center space-x-4' }}) }}
                    {{ form_rest(form) }}
                    <div class="flex flex-row space-x-2 justify-end align-bottom text-end items-end h-full">
                        <button class="text-black px-2 py-1 bg-white rounded-full">Rechercher</button>
                        <a href="/" class="text-black px-2 py-1 bg-white rounded-full"><i class="fa-solid fa-rotate-left"></i></a>
                    </div>
                {{ form_end(form) }}
            </div>
        {# </form> #}
    </div>
</div>